## What this changes

<!-- One or two sentences. -->

## Checks

- [ ] `python3 tools/check_house_rules.py` passes
- [ ] `python3 tools/check_imports.py` passes (no new dependency)
- [ ] `python3 tools/shamir_vectors.py` passes
- [ ] The app's self-tests pass on a DEBUG launch, or this change cannot affect them
- [ ] Nothing here weakens a promise in `docs/PRODUCT.md` section 7
- [ ] Copy reads plainly, says "key holder" and has no em dashes

## If this touches the release path

<!-- ReleaseMachine, ReleaseFeed, EstateLogVerifier, EstateKeys, the ceremony.
     Say what an attacker gains or loses, and which test proves it. -->
