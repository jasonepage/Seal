xcuserdata/
*.xcuserstate
.DS_Store
DerivedData/

# Founder perk minting — NEVER commit the key or unclaimed codes
tools/founder_private.pem
tools/mint_out*/

# Wrangler's local cache — holds the Cloudflare account id and the account
# email, changes on every deploy, and is rebuilt automatically. Never commit it.
.wrangler/

# Working notes, not part of the product
Claude outputs/
